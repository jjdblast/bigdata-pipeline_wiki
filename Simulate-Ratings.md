The following Akka App puts data from `datasets/dating/ratings.csv` onto the Kafka `ratings` topic to simulate real-time streaming ratings
```
root@[docker]$ $PIPELINE_HOME/flux-feed.sh
...Starting Feeder...
```